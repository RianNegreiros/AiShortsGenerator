services:
  app:
    image: backend 
    build:
      context: ./backend
      dockerfile: Dockerfile 
    ports:
      - "8080:8080"
    volumes:
      - ./backend/app.db:/app/app.db
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
